[defaults]
inventory = ./ansible_plugins
enable_plugins = aws_ec2
host_key_checking = False
private_key_file = /opt/private-key.pem

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false